export enum TargetType {
    SSM = "SSM",
    SSH = "SSH"
}

export enum SessionState {
    Active = "Active",
    Closed = "Closed",
    Error = "Error"
}

export enum IdP {
    Google = "Google",
    Microsoft = "Microsoft"
}